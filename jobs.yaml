- job-template:
    name: jobs_uploader
    project-type: pipeline
    pipeline-scm:
      scm:
        - git:
            url: https://github.com/nikitarredline/Jenkins
            branches:
              - main
      script-path: pipelines/jobs_uploader.groovy

- project:
    name: test_project
    jobs:
      - jobs_uploader