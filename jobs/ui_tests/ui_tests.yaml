- job:
    name: ui_tests
    project-type: pipeline

    parameters:
      - string:
          name: REFSPEC
          default: main

    pipeline-scm:
      scm:
        - git:
            url: https://github.com/nikitarredline/JavaQAProfessional
            branches:
              - $REFSPEC
      script-path: Jenkinsfile