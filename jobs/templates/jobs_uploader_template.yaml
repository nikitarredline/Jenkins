- job-template:
    name: jobs_uploader
    project-type: pipeline

    scm:
      - uploader_scm

    pipeline-scm:
      scm:
        - uploader_scm
      script-path: pipelines/jobs_uploader.groovy